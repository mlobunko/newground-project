import * as actions from "./actions";
import * as t from "./actionTypes";
import * as components from "./components";
import * as saga from "./saga";

export default { actions, t, components, saga };
